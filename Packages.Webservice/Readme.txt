Webservice for accessing (nuget) packages repository:
    - search
    - upload
    - download

It would contain nuget but only one version at time, and so upload will ensure dependancies are satisfied, and so upload needs to support multiple packages upload