- Must support client changes invokations
- OAuth backend, Database backend
- Multiple groups inheritance, Groups
- Permission set det
- Per user and per group permissions
- User authentication
- Per group administrators
- Permission must be indexable string (len 128 bytes like "product.service.operation.permissionEnabled"'), just keys no values
- When 
- Groups can be group members
- User authorization, if permission exists for more than user then map of the groups levels and permissinos must be return 

